import 'package:flutter/material.dart';

class SubscriberManage extends StatelessWidget {
  const SubscriberManage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('subscriber');
  }
}
